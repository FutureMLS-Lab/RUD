"""Kernel hub agent-runner helpers.

Packaged so the Loom-integration entrypoints (``rud_kernel`` / ``resolve_plugin``)
are importable as ``scaffold.agent_runner.*`` and exposed as console-scripts.
Loom's Kernel Lab invokes the bundled copy under ``loom/kernel_hub/``.
"""
